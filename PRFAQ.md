# NoEatToStopシステム

食べてなかったら止まるシステム

## 目的

子供が食事中にテレビを見ている時、もくもぐと食べる動作が止まったままテレビに意識が集中してしまう。食事がメイン、テレビはついでに情報取得という位置付けにしたいが、本来目的である食事に集中してほしい。そのため、食事の動作をしている時のみテレビがON、食事動作をしてない時はテレビがOFFとなるようにしたい。

以下ユーザーストーリーやシステム構築をするための計画と要件と設計をまず行う。

## ユーザーストーリー計画

以下ユーザーストーリーを想定してユーザーストーリー計画を完成させます。

作業の計画を立て、各ステップにチェックボックスを付けて ./construction/userstory_plan.md ファイルにステップを記載してください。
いずれかのステップで私の説明が必要な場合は、[Question] タグを付けて質問を追加し、私が回答を記入するための空の [Answer] タグを作成してください。
独自の判断や決定は行わないでください。計画を作成したら、私のレビューと承認を求めてください。
承認後、同じ計画を 1 ステップずつ実行できます。各ステップが完了したら、計画のチェックボックスに完了マークを付けてください。

1.カメラデバイスを接続したエッジデバイスは常に起動し、映像を取得する
2.ユーザーは食事の準備を始める。その手順をふまえ、次に食事が開始されるという条件とし、食事が開始された時は、食事開始により詳細な映像内容の判定を開始するようにする。
3.ユーザーの食事状態を数秒の映像を元に判定する。食事状態とは、食事しているか（食べているか）、食事していないか（食事の際の手や口がとまっているか）とする。
4.食事しているという状態から食事していない状態への状態変化とともに、テレビデバイスの電源をOFFする。
5.食事が片付けられると食事が終了となり、詳細な映像判定処理も解除される。



## システム構成と実装案作成

食事中の風景をカメラでとらえ、その動画から子供が食事中かどうか判定し、食事動作が途中で止まってしまった場合、テレビがオフとなるようなシステムを作る。
以下技術を使った実装の計画をたてる。

構成の計画を立て、各ステップにチェックボックスを付けて ./construction/architecture_plan.md ファイルにステップを記載してください。
いずれかのステップで私の説明が必要な場合は、[Question] タグを付けて質問を追加し、私が回答を記入するための空の [Answer] タグを作成してください。
独自の判断や決定は行わないでください。計画を作成したら、私のレビューと承認を求めてください。
承認後、同じ計画を 1 ステップずつ実行できます。各ステップが完了したら、計画のチェックボックスに完了マークを付けてください。

構成に登場する要素と条件

- カメラ映像取得: WebCamera(USB)をEdgeデバイスに接続
- Edgeデバイス: Raspverrypi (テスト時はMacOS) カメラ映像のエッジ側処理と映像の送信
- カメラ映像送信: Edge から S3 にフレーム画像を直接アップロード（presigned URL で管理画面に配信）
- 映像認識（Edgeデバイス): IoT Greengrass 上の ChewingAnalyzer（OpenCV Haar Cascade 顔検出 + 口領域差分 + 咀嚼判定）
- 映像認識 (AWS側): 将来拡張として Amazon Bedrock (Claude) による高精度分析を想定
- logと監視: Amazon Managed Graphana と QuickSightを利用
- 映像の保存: Edgeに数秒分の映像保存、AWS側に1食事分の映像を保存
- 設定の制御や映像確認画面: Vuejs v3 + tailwindcssのSPA を S3において、Cloudfront経由でアクセス
- 開発構成: CDKを使ったインフラとアプリのAWSデプロイ